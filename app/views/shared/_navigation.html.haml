%ul
  %li.main
    = link_to("FIND A JOB", url_for(jobs_path))
  %li.main
    = link_to("POST A JOB", url_for(new_job_path))
  %li.bids
    = link_to("ACTIVE", url_for(login_path))
  %li.profile
    = link_to("PROFILE", url_for(login_path))
  %li.login
    = logged_in? ? link_to("LOGOUT", url_for(logout_path)) : link_to("LOGIN", url_for(login_path))